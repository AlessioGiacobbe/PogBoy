pub mod interrupts {
    use crate::cpu::CPU::CPU;

    impl CPU<'_> {
        pub(crate) fn check_interrupts(&mut self) {

        }

        pub(crate) fn handle_interrupts(&mut self) {

        }
    }
}