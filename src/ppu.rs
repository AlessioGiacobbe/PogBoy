pub mod ppu {
    use piston_window::{clear, PistonWindow, rectangle, WindowSettings};

    pub struct PPU {
    }

    impl PPU {

        pub(crate) fn new() -> PPU {
           PPU {
           }
        }

    }

}